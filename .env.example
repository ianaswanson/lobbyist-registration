# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Database Configuration
# For local development, SQLite is used
DATABASE_URL="file:./dev.db"

# NextAuth.js Configuration
# IMPORTANT: Generate a secure random secret for AUTH_SECRET
# Run this command to generate a new secret:
#   openssl rand -base64 32
# Then paste the output below (keep the quotes)
AUTH_SECRET="REPLACE_WITH_OUTPUT_FROM_openssl_rand_-base64_32"

# Base URL for NextAuth.js
# For local development:
NEXTAUTH_URL="http://localhost:3000"
# For production, this should be set in Google Secret Manager or Cloud Run environment variables
